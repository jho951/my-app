import styled from "styled-components"

const HeaderWrap = styled.div`
  width: 100%;
  .header-title {
    font-size: 1rem;
    display: flex;
    justify-content: center;
    cursor: pointer;
  }
`

export {HeaderWrap}
